version: '3.8'

services:
  laravel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard-warehouse
    ports:
      - "2345:80"
    volumes:
      - ./storage:/var/www/storage
      - ./bootstrap/cache:/var/www/bootstrap/cache
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped